# Behavioral Deviations from the Unity BrawlerAGD (AIIDE '22)

The port preserves the Unity build's *design-space semantics* — same parameters, same
ranges, same FSM rules, same AI playtester decision tree, quirks included. This page is
the complete ledger of **intentional differences**, so results produced with this system
can be interpreted against the published AIIDE '22 findings. Anything not listed here is
either bit-parity by construction or an unintentional bug (please report it).

Bottom line for comparability: **fitness scores from this system are NOT directly
comparable to the paper's numbers** (items 1–2 alone change the fitness landscape).
Within this system, all results are exactly reproducible from (genome, seed).

## Fitness function (`standard-v2`)

| # | Change | Unity behavior | Why it matters |
|---|--------|----------------|----------------|
| 1 | Stock-fairness term computes \|s1 − s2\| | `Math.Abs(s1 - s1)` ≡ 0 — the term was a constant 3 and never influenced selection | Evolution now actually rewards stock-even matches. Published runs evolved without this pressure. |
| 2 | Damage cap counts total stocks lost `(6 − s1 − s2)` | Sign error `(6 − s1 + s2)` inflated the cap whenever P2 had stocks left | The anti-"corner grinding" penalty now engages as designed; high-damage no-kill games score lower than they did in the paper's runs. |

## Simulation & physics

| # | Change | Unity behavior | Why it matters |
|---|--------|----------------|----------------|
| 3 | Fixed 60 Hz tick; all durations in integer ticks | Gameplay logic ran per rendered frame (variable), physics at 50 Hz, move timings on wall-clock coroutines | Removes frame-rate dependence entirely; matches are a pure function of (genome, inputs, seed). |
| 4 | Blast zone fixed to 16:9-equivalent constants (±10.76 × ±5.5) | Sized from the camera at runtime — **depended on the player's window aspect ratio** | Every match plays in the same arena, headless or rendered, any monitor. |
| 5 | Movement clamp fixed | Tapping the opposite direction snapped a character to full speed the other way; self-movement input could bleed off knockback speed above the cap | Fixes Unity defect #4 (plan §1). Movement feels crisper; knockback momentum is preserved while held toward. |
| 6 | Being hit cancels an in-flight move | The move coroutine kept firing state changes after a stun (race), occasionally re-entering attack mid-stun | Deterministic FSM; standard fighting-game semantics. |
| 7 | Leaving the ground mid-move no longer aborts the move state | `OnCollisionExit2D` yanked state to Air regardless of current state, racing the move coroutine | Moves run warm-up → execute → cool-down to completion, then resolve by grounded/jumps. |
| 8 | Single clean hit path with 6-tick invincibility | Damage logic duplicated across Enter/Stay/Exit trigger events with inconsistent stat counting (double-count risk) | One hit = one damage application = one stat increment, always. |
| 9 | Solid player-vs-player contact: movement clamps at the opponent's body with mass-weighted momentum transfer; residual overlaps separate ≤0.05 u/tick | Box2D iterative contact solve (soft, impulse-based) | Same emergent behavior (push, stand on heads, no pass-through, no teleports) via a deterministic model. Contact stiffness is tunable (`MatchConfig.MaxDepenetrationPerTick`). |
| 10 | Ground detection: feet-on-platform-top test | Raycast + collision-event patchwork; documented Unity bug: jumps refreshed when a platform was above AND below | Fixes Unity defect #5. |
| 11 | "3 stocks = 4 lives" **preserved** (not a deviation — noted to prevent "fixing") | `respawn()` ends the game only when dying at 0 stocks | Matches the shipped game and the study's four-stock survival description. |

## Generation & genome

| # | Change | Unity behavior | Why it matters |
|---|--------|----------------|----------------|
| 12 | Stage generator sizes Above-children parent-relative | `rand.Next(2, platform.xSize - x + 1)` used **absolute** x — children of negative-x parents could exceed the parent width and the design-space max | Generated stages stay within the intended design space. Imported legacy stages are untouched (data loads verbatim). |
| 13 | Knockback params: generation range vs valid domain split | Unity re-saved post-flip/post-constraint values silently outside the generation range | Formalized: `knockbackModX/Y` generate in [0,1]/[−1,1] but validate in ±1.5 (the constraint lerp's convex hull). Discovered from the study games' own files. |
| 14 | Move 2 / shield not ported | Half-implemented: parry/reflect fields no code read, shield break did nothing | Per designer decision; returns later as a schema-driven move type. |

## Determinism & evaluation

| # | Change | Unity behavior | Why it matters |
|---|--------|----------------|----------------|
| 15 | All RNG seeded (PCG32; per-match streams derived from run seed) | GA time-seeded; AI used a second, unseeded RNG | Any match or whole run is exactly reproducible; every fitness score is replayable from its input trace. |
| 16 | Evaluation is parallel and order-independent | Sequential, real-time, one Unity scene at a time (~12 h/run) | ~50,000× faster per core; replicate studies are practical (minutes). Parallelism provably does not change results (tested). |

## Controls & input (2026-07-09, docs/features/multi-move-controls.md)

| # | Change | Unity behavior | Why it matters |
|---|--------|----------------|----------------|
| 17 | Input model: 4 assignable action buttons + captured (inert) vertical axis; genome gains a button→move mapping gene; keyboard scheme is WASD + Space + IJKL, P2 is gamepad-only | One attack button (P1: S, P2: K); W/I jumped; no button assignment concept | Foundation for evolving multiple moves per character. The agent's DECISIONS are unchanged — it presses the lowest button mapped to move 0 instead of "attack" — and for single-move genomes (everything the paper studied) behavior is provably identical: pre/post evaluation of Games A/C/F × 2 seeds reproduced all 30 rounds' stats exactly, and genome-generation RNG streams are untouched (button genes consume RNG only when >1 move exists). Trace format grew 3→7 values/player (old traces load and replay bit-identically); game.json is formatVersion 2 (v1 loads with all-buttons→move-0). When multi-move genomes arrive, agent move SELECTION will be new instrument behavior and gets its own entry. |

## Instrument change (2026-07-09, docs/features/utility-agent.md)

| # | Change | Unity behavior | Why it matters |
|---|--------|----------------|----------------|
| 18 | **The fitness instrument is now the UtilityAgent**, replacing the ported decision tree as the default playtester everywhere (evolution, CLI, app). Channel-based utility scores normalized to 1, argmax/proportional selection mixture (randomness 0.15), 8-tick decision commitment, attack-position seeking, threat dodge with trade commitment, damage-scaled evasion, reachability-gated recovery. Per-run config recorded in run.json; pre-2026-07-09 checkpoints resume under the DT. | The AIIDE '22 decision tree (level-held inputs, absolute-y checks, origin-homing recovery) | **Fitness scores under the two instruments are not comparable** — a deliberate research pivot, designer-directed. Full comparison: docs/reports/2026-07-09-utility-agent-comparison.md. Notable consequences: DT-era deaths often came from the DT's own bad recovery; the utility agent recovers competently, so fragile genomes time out (punished by fitness) instead of self-destructing; degenerate genomes (knockback-cancel stun-locks) are now exploited and heavily punished rather than masked by agent noise; the instrument is stochastic, so measured fitness carries sampling noise (median-of-rounds mitigates). The DT remains in-tree for comparison and its golden test; archival planned after designer confirmation. |

## Shield move type (2026-07-12, docs/features/shield.md)

| # | Change | Unity behavior | Why it matters |
|---|--------|----------------|----------------|
| 19 | Shields implemented as a schema-driven move type: new `PlayerState.Shield` (cyan tint), guaranteed third move slot, nine evolvable parameters, coverage-based blocking, spacing push (new step in the contact phase of the tick order), hold/hit degradation with regen, and a cap-EXEMPT evolvable break stun. Agent gained shield raise/hold/aim/release behaviors and the dodge-vs-shield weighted-random arbitration; game.json is formatVersion 3. | Unity's Move 2/shield was half-implemented dead code (parry/reflect fields nothing read — deviation #14) | Completes the deferred shield from #14 as a genuinely searchable design-space axis. Instrument change (agent) + genome-structure change: fitness results are a new era; standard-v3 is deliberately BLIND to shields in v1 (stats recorded: activations/blocks/breaks/shield ticks). Finding: under damage-rewarding blind fitness, shield use is selected AGAINST — see shield.md. |

## Dash move type (2026-07-13, docs/features/dash.md)

| # | Change | Unity behavior | Why it matters |
|---|--------|----------------|----------------|
| 20 | Dash move type: `PlayerState.Dash` (orange tint) with warm-up/travel stages; gravity suspended during travel (a deliberate physics exception); per-stage evolvable invulnerability; air budget extended to jumps + one dash per airtime (all orderings); dash-contact velocity capped damage-independent (shove, never KO); guaranteed last move slot pinned to the last button. Agent: unified defense channel (nothing/hop/shield/dash, one weighted-random pick), recovery/approach/punish dashes. game.json v4. | No dash concept | New design-space axis; instrument change (defense refactor). With 4 moves on 4 buttons + the pin, the button-mapping gene is a fixed bijection until dynamic composition. Fitness remains dash-blind; smoke runs show immediate adoption (mobility pays indirectly) and active search over the invulnerability genes. |

## Fast fall / crouch / directional influence (2026-07-13, docs/features/fastfall-crouch-di.md)

| # | Change | Unity behavior | Why it matters |
|---|--------|----------------|----------------|
| 21 | Seven character-schema appends (fastFallAcceleration, crouchAccelerationChange, crouchSpeed, crouchMoveSpeed, crouchHeightRatio, directionalInfluence, diKnockbackReduction; neutral loader defaults, no format bump). Fast fall: flat added downward acceleration while holding down airborne (Air/AirJumpsExhausted/WarmUp/CoolDown). Crouch: new `PlayerState.Crouch` (purple tint), Sink/Held/Rise stages, feet-planted height scaling (the crouched hurtbox IS the body — ducking under high arcs is structural), slide friction ±, positive slides capped at 1.5× ground speed; movement and all actions allowed while crouched (actions queue through the uncancellable input-deaf Rise). DI: at the hit instant the victim's held direction deflects knockback by ≤10% of its magnitude and near-opposite holds cut it by ≤20%; skipped for any hit taken while shielding (incl. pokes). Agent: Vertical is a real decision channel; the defense pick grew to six options (+fast fall, +crouch-under-when-it-clears-the-arc); DI pre-positioning toward stage center under threat — imperfect by construction (commitment window + randomness). HUD gained a live per-player held-direction arrow that flashes on the tick a hit lands. | No fast fall, crouch, or DI concepts | First CHARACTER-level (not button-mapped) mechanics of Phase 6; instrument change (defense channel, vertical channel). Fitness stays blind (stats: FastFallTicks/CrouchTicks/DIInfluencedHits). Smoke runs: fast fall adopted immediately and heavily, DI live on most hits, crouch situational; the fitness plateau sits LOWER than the pre-feature baseline because DI + fast-fall recovery shorten juggle strings — the per-stock damage term binds sooner. Scores are not comparable across the feature boundary. |

## Composition control + advanced ranges (2026-07-14, docs/features/evolve-composition-and-ranges.md)

| # | Change | Unity behavior | Why it matters |
|---|--------|----------------|----------------|
| 22 | The evolve menu (and CLI) can now select button composition — PINNED (the fixed attack/attack/shield/dash layout, still the default), RANDOM (each of the four buttons holds an evolvable move-type gene), or PER-BUTTON (pin some, free others) — and adjust per-parameter generation ranges, including CLAMPING (min = max) and ranges beyond the tested valid domains (valid domain widens to match; UI warns). In composed modes: one move per button (identity mapping — the permutation gene is meaningless and retired there), types re-roll under mutation at `typeRerollRate` (whole-slot regeneration), mismatched-type slots inherit wholesale in crossover (the rule shipped with shields). run.json records composition + overrides; resume honors them. | Unity had one fixed move; no composition or range concepts | The genome's STRUCTURE is now searchable, not just its parameters — a design-space expansion, not an instrument change (the agent is untouched; the PINNED path is byte-identical, proven by the unchanged population fingerprint golden). First finding: under shield/dash-blind fitness, freed compositions outperform pinned (~76 vs ~64 top) and different seeds converge to different compositional motifs (attack+dash vs attack+shield worlds) with asymmetric champions — scores are only comparable between runs sharing a composition setting AND range set (both recorded in run.json). Zero-attack characters are legal and expected (~20%/character in RANDOM); sim, agent, fitness, and GenomeDistance all already tolerated them. |

## Projectiles (2026-07-14, docs/features/projectiles.md)

| # | Change | Unity behavior | Why it matters |
|---|--------|----------------|----------------|
| 23 | Projectiles as a 24-gene attack-family move type (`MoveType.Projectile`, game.json v5) and the FIRST NON-PLAYER ENTITIES in SimWorld: a spawn-ordered projectile list stepped in a new tick phase (player physics → projectiles → melee hits), with CLOSED-FORM paths (linear / sine / quadratic ± gravity ± acceleration — position is a pure function of age, never integrated), real rotated-shape collision (circle / OBB / triangle via SAT), melee-pipeline hit resolution (same knockback formula, shield coverage blocking, DI, capped stun; dash i-frames negate and the bolt passes through), owner launch immunity then a hitsSelf gene, and despawns on TTL / decay-to-zero / blast boundary / platform contact (platforms DESTROY bolts) / any hit. No live cap (designer). StateHash appends the projectile section ONLY when bolts are live — projectile-less matches hash identically, so match goldens did not move (the population fingerprint re-pinned for serialization bytes only). Agent: loose-corridor fire-from-range with a hard close-range gate, moderate zoning weights, and incoming-bolt prediction feeding the six-option defense channel — which triggers REGARDLESS of counter-hit options (a bolt in flight is committed damage; counter-firing doesn't stop it, unlike the melee trade-commit). `SlotSpec.Projectile` joins the RANDOM pool (uniform over four types — 3-type-era composed runs archived as non-regenerable). | No projectile concept | Ranged combat as a searchable design-space axis; instrument change (defense trigger asymmetry, projectile threat prediction). Fitness stays blind (stats: ProjectilesFired/ProjectileHits; blocks fold into BlockedHits). Smokes: with the per-button pin the plateau drops (~61 vs ~88 baseline — a melee slot became a harder-to-confirm ranged one), the random pool sits between (~76) and gives projectiles 38% of the final population with a 3-projectile-zoner vs 3-dash-rusher champion. One tick of Idle separates consecutive dashes — a bolt found that gap in testing; continuous i-frames are NOT a thing, kept deliberate. |

## Projectile wind-up telegraph (2026-07-20, designer-directed)

| # | Change | Unity behavior | Why it matters |
|---|--------|----------------|----------------|
| 24 | A winding-up PROJECTILE now telegraphs exactly like a melee wind-up (designer: "warm-up phases signal defensive counterplay across the board"). The telegraph "arc" is the shot's predicted corridor at the defender's column — the same loose test the shooter aims with, seen from the receiving end; crouch-clear uses the corridor bottom like the melee arc bottom. The melee trade-commit gate applies (interrupting the shooter cancels the shot); an in-flight bolt still triggers defense UNCONDITIONALLY (#23). | n/a | Instrument change closing a spec gap: the shield spec always said "shield when you see a wind-up," but zoners never produced one — measured consequence was ZERO shield activations against a projectile champion (shield-vs-zoner franken match). With the telegraph the same match shows real raises and blocks; whether shield ADOPTION recovers under evolution is an open question the tele5 smokes measure. |

## Five assignable buttons (2026-07-20, designer-directed)

| # | Change | Unity behavior | Why it matters |
|---|--------|----------------|----------------|
| 25 | Jump reduced to a SINGLE button (pad B; Space), freeing pad Y: `InputFrame.ActionCount` 4 → 5. New layout: I/J/K/U/L keys ↔ L1/X/A/Y/R1 — U/Y is the NEW index 3 so the L/R1 button stays LAST, preserving the dash pin's physical home. game.json v6 (buttonMoves 4 → 5; ≤5 files migrate: new index 3 duplicates button 0's move, old index 3 → new index 4) with the trace reader migrating 7-value rows the same way (old a3 → new bit 4) — a real 4-button-era champion + trace replays BIT-IDENTICALLY under both builds (hash-verified). Composed-era checkpoints with 4-slot compositions cannot resume (explicit error; archived). Population fingerprint re-pinned — a REAL generation change: the pinned mapping gene draws over four mappable buttons (was three). Match goldens unmoved. | One attack button | The design space widens: five slots for four move types means composition can double up on types even in per-button mode, and the pinned layout gains a fifth slot to assign. All pre-change runs lose generation-reproducibility (fitness comparability across the boundary was already broken per-feature); their artifacts stay loadable and replayable. |

## Reflect genes (2026-07-20, designer-directed)

| # | Change | Unity behavior | Why it matters |
|---|--------|----------------|----------------|
| 26 | `reflect` appended to BOTH the shield schema (10th gene) and the dash schema (6th): a reflecting shield re-fires a fully-covered PROJECTILE at its shooter instead of absorbing it; a reflecting dash re-fires any bolt contacting the dasher during the Dash state (checked before, and independent of, the i-frame genes). Reflection semantics: ownership transfers to the reflector (return bolts hit the original shooter and credit the reflector), Origin/Facing re-seat and the PATH clock restarts, while the TTL and damage-decay clocks continue from the original launch (designer spec). The owner-clearance latch prevents ping-pong; pokes still poke; melee is untouched; a shield reflect still degrades the shield like a block (open judgment call). Agent: reflect-equipped shield/dash defense options score ×1.5 against RANGED threats (new RangedThreat context flag). View: reflected bolts strobe white; dash i-frames strobe the body alpha (distinct from post-hit invincibility). Stat: ProjectilesReflected; fitness stays blind. Loader defaults keep every pre-append file at reflect-off; fingerprint re-pinned (one extra generation draw per shield/dash); match goldens unmoved. | No projectiles, no reflection | A defensive counter that converts zoning pressure into return fire — the designer's hypothesis is that reflectability raises defensive shield/dash adoption; the reflect smokes measure it. SimProjectile's origin/facing/owner became mutable state (hashed accordingly). |

## Map Size (2026-07-21, designer-directed)

| # | Change | Unity behavior | Why it matters |
|---|--------|----------------|----------------|
| 27 | The stage grew an 11-gene ParamSet (`stage` schema, game.json v7): visible half extents (0.5×–5× legacy, width/height independent), KO margin, platform count/max-size budgets, `mirrored`/`mirrorSide`, and four spawn genes. The BLAST ZONE is now genome-driven — visible × (1 + koMargin), reproducing the legacy constants BIT-EXACTLY for pre-v7 files (raw-bit regression test). Spawns are genes repaired at the GENETIC-OPS layer (never at sim time — pre-v7 artifacts replay untouched): inside the visible box, over a platform, in a column the player's body clears (an embedded body gets ejected by the axis-clamp physics, on narrow maps past the KO line — found and pinned by generator probes). The generator grows Left/Right/Above/Below within jump reach (traversable by construction), rejects overlaps, re-seeds its growth stack (the Unity tree died stack-empty: 25% of stages were a lone mirrored pair; now 96% mean budget fill), regrows layouts with no body-safe spawn column, and generates asymmetric stages when `mirrored` is off. Stage MUTATION is no longer always full regeneration: params mutate first, and a mutated-ON `mirrored` gene TRANSFORMS an asymmetric layout per `mirrorSide` (clamp source half at the axis, reflect) instead of discarding it; otherwise the layout regenerates from the mutated params. AGENT instrument change: the 20×15 platform-sense box scales UP with map size (never down; exactly Unity's box on ≤legacy maps). `MaxMatchSeconds` default 60 → 300 (run.json records per-run values; old checkpoints resume at their recorded 60). Fitness stays blind to every map gene. Fingerprint re-pinned (3×, same day); both match goldens re-pinned for the timeout only — verified bit-identical to the prior pins at 60 s. | Fixed ~19.6×10 arena sized by the WINDOW ASPECT (blast width carried 1.1 twice), 3-platform mirrored tree, derived spawns, 60 s cap | The single largest design-space expansion since the port: map scale, symmetry, density, and spawn placement are all searchable. Replicate runs already diverge into distinct map motifs (tall mirrored towers vs sprawling asymmetric arenas). Fitness values across the boundary are not comparable (bigger maps change KO dynamics and match length); on >legacy maps the scaled sense box changes what the fitness instrument measures — legacy-size maps measure exactly as before. |

## Agent air-jump conservation (2026-07-22, designer-reported)

| # | Change | Unity behavior | Why it matters |
|---|--------|----------------|----------------|
| 28 | Two UtilityAgent refinements stop the agent WASTING its air jump, which fixed a large-map oscillation the designer observed (agents pacing back and forth faster than human input, never attacking): (a) `ThreatDodgeBehavior` adds the escape hop ONLY when grounded — an airborne flinch-dodge no longer spends the second jump; (b) traversal requires a jump only for a real height gain (> 0.5 u) or a real horizontal gap (> 0.5 u), so the agent WALKS between adjacent same-height platforms instead of hopping. Root cause: on wide/tall maps the agent is airborne most of the approach; the pre-fix behaviors burned the air jump every approach, stranding it in `AirJumpsExhausted` — a state that can neither attack (FSM, Unity parity) nor jump — so it drifted back, landed, re-approached, and dodged again forever (the `ExhaustedCaution` retreat supplied the reversal). Measured over 30 seeds/map: exhausted-time fell from 65%→45% (wide), 72%→20% (tall), 47%→21% (a legacy game), damage output rose almost everywhere, and a champion that timed out 22/30 now KOs 30/30. The flagship legacy game (GameC) is materially unchanged (37% exhausted, similar attacks/damage) but individual decisions differ at the bit level, so the utility golden re-pins. The decision-tree instrument and the genome fingerprint are untouched (agent-independent). | n/a — the utility instrument is a Godot-era addition (DEVIATIONS #18) | Instrument change: it alters what fitness measures on EVERY map, though the intent is fidelity — a human playtester does not burn a double-jump to flinch, nor hop across contiguous ground. Legacy dynamics look preserved (GameC steady); a full replication re-run is the way to confirm the plateau + both motifs still reproduce if the designer wants that certainty before relying on cross-boundary fitness comparisons. Regression-guarded by UtilityAgentTests.TraversalWalksBetweenAdjacentSameHeightPlatforms. |

## Spawning Behaviors (2026-07-22, designer-directed)

| # | Change | Unity behavior | Why it matters |
|---|--------|----------------|----------------|
| 29 | Characters spawn on a temporary platform, invulnerable, replacing the instant respawn (docs/features/spawn-and-polish.md; two evolvable stage genes, game.json v8). Two distinct conditions: **intangible** (ignores damage AND character collision — the opponent phases through, cannot push you off) while on the spawn pad, ending on leaving the pad / the platform timer / attacking; and **invulnerable** (ignores damage, KEEPS collision) on its own timer, ending strictly on the timer. Respawns get a 3 s absent blackout first; match start puts both on their pads with no blackout. The pad is a per-player temporary platform solid to its owner only (the opponent's physics never receives it). AGENT change: the utility instrument treats a damage-immune opponent as unhittable ("shouldn't attempt to attack an invulnerable enemy") — gated on the spawn immunity only, not the 0.1 s post-hit invincibility, so legacy matches and their goldens are untouched. The whole feature gates on `spawnFeatureActive` (either duration > 0), a per-level constant: OFF ⇒ instant vulnerable respawn, byte-for-byte the pre-feature sim, INCLUDING the state hash (the spawn section is a gated suffix like projectiles). Legacy/imported games default both durations to 0, so every existing replay, both match goldens, and resumed checkpoints are bit-identical; only the population fingerprint re-pins (two new generation draws). | Instant respawn at a fixed point, immediately vulnerable | Introduces spawn protection the paper's build lacked, changing early-life dynamics on new games. Fitness stays blind (spawn-invuln time is a research stat only). The designer's stated worry — a player attacking safely from invulnerability ("spawn-camping," since invuln follows its timer regardless of attacking) — was measured over an 80-generation evolved population: 0.4% of all damage was dealt by an immune attacker, so the degenerate strategy did not emerge; evolution settled on modest durations (platform ~2.4 s, invuln ~1.2 s). Retunable via the stage-gene ranges or an attack-ends-invuln rule if a later run turns degenerate. |

## Per-character platform fit (2026-07-23, designer-directed)

| # | Change | Unity behavior | Why it matters |
|---|--------|----------------|----------------|
| 30 | After a stage and both characters are known, `StageRules.FitToCharacters` MOVES platforms so both characters can traverse every intended edge and no gap is asymmetrically body-passable (docs/features/spawn-and-polish.md §Platform fit). Deterministic and RNG-FREE — it edits platform coordinates in place, never re-rolling, so the generation stream stays aligned (only coordinates change; the fingerprint re-pins, match goldens unmoved). Runs in Generate/Crossover/Mutate. The reach model mirrors the agent's PlatformGraph EXACTLY (jumps + air speed + scaled gravity, no dash), so a fitted stage is one the agent's own pathfinder treats as connected. **Amended 2026-07-27** (designer: asymmetric gaps still appeared in play): the body-gap pass became an ITERATIVE five-strategy solver (widen either side / dock either side / vertical separation; re-scans after every move; per-pair attempt rotation + force-dock + an unresolvable set make it terminate by construction; moves must not shrink both-character connectivity). Audited over 800 seeds: asymmetric corridors 248 stages → ZERO with connectivity unchanged and zero overlaps; fingerprint re-pinned again; fair stages still returned untouched. | The Unity/earlier generator was character-BLIND (abstract jump grid), so a stage could favour one character's mobility over the other's. | Changes generation geometry (a fitness-instrument input): platform layouts now depend on the paired characters, coupling stage to characters at generation time. Post-amendment, no generated/bred stage carries a corridor one body passes and the other cannot; ~8% of dense layouts remain both-disconnected somewhere (unchanged by the amendment — the agent's "doomed" logic covers a stranded character). Trial evolutions healthy (runs/media/charts/gap-solver-trajectories.png). Property-tested: StageRulesTests.GeneratedStagesNeverHaveAsymmetricBodyGaps. |

## Air-budget exhaustion rule (2026-07-23, designer-directed)

| # | Change | Unity behavior | Why it matters |
|---|--------|----------------|----------------|
| 31 | The `AirJumpsExhausted` state — and its movement-only lockout — now requires the WHOLE air budget: the air jump AND the air dash, when the character has one ("we should only exhaust on jump, jump and dash"). Jumps spent with a dash still in hand keeps the character in `Air` with FULL air abilities (attacks included); the dash resolving airborne then lands in the exhausted state. This supersedes the 2026-07-13 rule that a dash in hand must not re-enable chasing — that rule's premise was "cannot attack in that state," which no longer holds: with a dash in hand the character CAN attack, so agent chasing there is legitimate (the state-keyed agent reads all stay semantically correct unchanged; the exhausted-disengage caution now keys on full exhaustion by construction). Dash-LESS characters reduce exactly to the old jumps-only rule, bit-for-bit: both match goldens, the utility golden, the DT instrument, and the fingerprint are ALL unmoved (verified — first suite run after the change was green with no re-pins). `SimPlayer.FullyAirExhausted` is the single predicate; the dash-entry branch in the exhausted FSM case became unreachable and was removed. | One air jump then exhausted (no dash existed) | Gameplay + instrument change on every dash-carrying genome: the jumps-spent-with-dash window (measured ~666 ticks in one showcase match) changes from movement-plus-dash-only grey to full-ability green, altering both what players can do and when the agent chases. Trial evolutions (2 × 300 gens, runs/exh-3101/-3102, chart runs/media/charts/exhaustion-rule-trajectories.png) show healthy climb-and-plateau dynamics. Legacy/paper genomes are dash-less, so replication results are untouched. Regression-guarded by DashTests.JumpsSpentWithADashInHandIsNotExhausted. |

## Known intentional quirk preservation (unchanged, for the record)

- AI playtester ported verbatim including: absolute-Y target comparisons, level-based
  jump input (instant ground→air jump chaining), the 20×15 platform sense box (scaled
  up on >legacy-size maps since #27 — exactly 20×15 on legacy maps), recovery homing
  toward world origin when no platform is sensed, recovery-tick stat counting.
- The 45°-knockback flip that produced the paper's "backwards knockback" player
  confusion is preserved (changing it is a design decision, not a port decision).
- Move hitboxes inherit the owning character's scale (big characters swing bigger).
- Single-point crossover semantics (point 0 = full copy of parent B), 5 mutation
  re-rolls **with replacement**, all-or-none whole-game mutation roll. (Stage mutation
  was full regeneration until #27 — it now mutates stage params and only regenerates
  or mirror-transforms the layout.)
